import { FC } from "react";
import "./styles.scss"

const Spinner: FC = () => {
    return (  
        <div className="container-spinner">
            <div className="spinner"></div>
        </div>
    );
}
 
export default Spinner;
